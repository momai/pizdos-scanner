name: pizdos-scanner

services:
  pizdos-scanner:
    image: ghcr.io/momai/pizdos-scanner:latest
    build: .
    pull_policy: always
    init: true
    network_mode: host
    stop_signal: SIGINT
    stop_grace_period: 15s
    working_dir: /app
    cap_add:
      - NET_RAW
    volumes:
      - ./geoip.dat:/app/geoip.dat:ro
      - ./config.toml:/app/config.toml:ro
      - ./db:/app/db:ro
      - ./results:/app/results
    command: ["geoip-scan", "ru"]
